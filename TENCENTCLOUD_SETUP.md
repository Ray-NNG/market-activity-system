# 🔧 腾讯云 COS 部署前置步骤

## ⚠️ 重要：需要你手动完成的步骤（5分钟）

部署脚本无法自动创建存储桶（需要 Web 登录验证），请按照以下步骤手动创建：

### 步骤 1: 登录腾讯云控制台

1. 打开 https://console.cloud.tencent.com
2. 用你的账号登录

### 步骤 2: 创建 COS 存储桶

1. 左上角搜索 **"COS"** 或点击左侧菜单 **存储 → 对象存储**
2. 点击 **创建存储桶**
3. 填写配置：
   - **存储桶名称**: `market-activity-system`
   - **所属地域**: **北京 (bj)**
   - **访问权限**: **公有读私有写**（或选择后续配置）
   - 其他选项默认即可
4. 点击 **创建**

### 步骤 3: 配置 CORS（跨域）

1. 进入刚创建的存储桶
2. 左侧菜单 → **安全管理 → 跨域 (CORS)**
3. 点击 **编辑**，添加规则：

```
允许的来源: *
允许的方法: GET, PUT, POST, DELETE, HEAD
允许的头部: *
公开头部: ETag, x-cos-request-id
```

4. 点击 **保存**

### 步骤 4: 记录你的账户信息

登录后，点击右上角账号 → **基本信息**，获取：
- **账户 ID**: 用于构建 COS 域名（可选）

### 完成后

返回告诉我 "✅ 已完成前置步骤"，我就会继续执行部署脚本！

---

## 📊 COS 成本预估

| 操作 | 费用 |
|------|------|
| 存储空间（1GB） | ¥0.03/月 |
| 请求次数（100万次） | ¥0.01/月 |
| 域名访问 | 免费 |
| **总计** | **¥0.1-1/月**（基础版）|

---

## 🔐 权限说明

### 为什么要创建子账号？（可选但推荐）

为了安全，建议为部署脚本创建一个**仅限 COS 权限**的子账号：

1. 右上角 → **访问管理**
2. **用户 → 新建用户**
3. 填写用户名：`cos-deploy`
4. 勾选 **编程访问**
5. 为用户赋予权限：**COS 读写权限**
6. 保存 `SecretId` 和 `SecretKey`

这样即使密钥泄露，攻击者也只能访问 COS，不能访问其他资源。

---

**前置步骤完成预计时间: 5-10 分钟** ⏱️
