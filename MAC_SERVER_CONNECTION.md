# Mac 连接到服务器完整指南

## 📍 你的服务器信息
- **地址**: 192.168.1.112
- **Nginx 端口**: 5666
- **用户**: huangshengwei
- **密码**: cvte2020

---

## 方案 1️⃣：最简单 - 通过浏览器访问（推荐）⭐⭐⭐

### ✅ 适用场景
- 只需上传文件或查看服务器状态
- 不需要终端命令
- 最简单快速

### 步骤

**第 1 步**：打开 Safari 或 Chrome

**第 2 步**：访问 Nginx 页面
```
http://192.168.1.112:5666/
```

**第 3 步**：查看是否能看到服务器欢迎页面

这就完成了！你已经连接到服务器。

---

## 方案 2️⃣：用终端 SSH 连接（推荐用这个执行部署）⭐⭐⭐

### ✅ 适用场景
- 需要在服务器上运行命令
- 执行部署脚本
- 管理文件和配置

### 前置条件检查

```bash
# 检查你的 Mac 是否已安装 SSH（99% 已装）
which ssh
# 应该显示：/usr/bin/ssh
```

### 连接步骤

**第 1 步**：打开终端

可以用以下任一方法打开终端：
- 按 `Cmd + Space`，输入 `terminal`，回车
- 或在 Launchpad 中找到"终端"
- 或在 Applications → Utilities 中找到 Terminal

**第 2 步**：尝试 SSH 连接

```bash
# 先尝试标准 SSH 端口 22
ssh huangshengwei@192.168.1.112
```

输入密码：`WKi3,Ly{1g`

### 如果连接成功 ✅

你会看到类似这样的提示符：
```
huangshengwei@server:~$
```

恭喜！现在你可以在服务器上运行任何命令了。

### 如果连接失败 ❌

尝试这些排查步骤：

#### 方法 1：检查网络连接
```bash
# 检查能否 ping 到服务器
ping 192.168.1.112
```

如果显示 `100% packet loss`，说明**网络不通**：
- 检查 WiFi 是否连接到同一网络
- 检查服务器是否开启
- 检查路由器设置

#### 方法 2：检查 SSH 服务状态
如果能 ping 通但 SSH 连接失败，可能是：
- SSH 服务未启动
- 防火墙阻止
- SSH 端口不是 22

告诉我具体的错误信息，我可以帮你进一步诊断。

---

## 方案 3️⃣：用 Finder 中的 SFTP 连接（拖拖拽拽上传文件）⭐⭐

### ✅ 适用场景
- 需要图形界面上传/下载文件
- 不想用命令行
- 管理和组织文件

### 连接步骤

**第 1 步**：打开 Finder

**第 2 步**：按 `Cmd + K` 打开"连接服务器"对话框

**第 3 步**：输入服务器地址

```
sftp://huangshengwei@192.168.1.112:22
```

**第 4 步**：点击"连接"

**第 5 步**：输入密码
```
WKi3,Ly{1g
```

### 连接成功后

Finder 会像挂载一个网络驱动器一样显示服务器文件系统。你可以：
- 拖拽文件上传
- 拖拽文件下载
- 新建文件夹
- 删除文件

---

## 方案 4️⃣：用第三方 SFTP 工具（功能最全）⭐

### 推荐工具

#### 1. **Transmit 5**（最强大，但付费 $45）
- 强大的文件管理
- 漂亮的界面
- 支持云存储

#### 2. **Cyberduck**（免费开源）⭐⭐⭐ 推荐
- 免费且功能完整
- 易用的拖拽界面
- 支持 SFTP、FTPS、S3 等

**安装 Cyberduck**：
```bash
brew install cyberduck
```

或者从官网下载：https://cyberduck.io/

**用 Cyberduck 连接**：
1. 打开 Cyberduck
2. 点击 "新增连接" → 选择 SFTP
3. 输入服务器信息：
   - 主机：`192.168.1.112`
   - 用户名：`huangshengwei`
   - 密码：`WKi3,Ly{1g`
   - 端口：`22`
4. 点击连接

---

## 方案 5️⃣：用 VS Code 远程开发（码农最爱）⭐⭐⭐

### 适用场景
- 直接在 VS Code 中编辑服务器文件
- 在 VS Code 中直接执行命令
- 完整的开发体验

### 安装步骤

**第 1 步**：打开 VS Code

**第 2 步**：安装 "Remote - SSH" 扩展
- 点击左侧扩展图标（或 `Cmd + Shift + X`）
- 搜索 "Remote - SSH"
- 安装官方的扩展（Microsoft）

**第 3 步**：连接到服务器
- 按 `Cmd + Shift + P` 打开命令板
- 输入 "Remote-SSH: Connect to Host"
- 选择 "Add New SSH Host"
- 输入：`ssh huangshengwei@192.168.1.112`
- 选择 SSH config 文件位置
- 再次打开命令板，选择你刚添加的 Host 连接

**第 4 步**：输入密码
```
WKi3,Ly{1g
```

### 连接后

VS Code 会直接在服务器上工作，你可以：
- 编辑文件
- 打开集成终端在服务器上运行命令
- 安装服务器端扩展
- 调试代码

---

## 🚀 执行部署的完整步骤

现在你知道怎么连接了，来执行部署吧！

### 推荐方式：用 SSH 终端执行

**第 1 步**：打开终端，SSH 连接到服务器
```bash
ssh huangshengwei@192.168.1.112
```

输入密码：`WKi3,Ly{1g`

**第 2 步**：粘贴这个命令（一行）
```bash
bash -c "$(curl -fsSL https://raw.githubusercontent.com/Ray-NNG/market-activity-system/main/server-install.sh)"
```

**第 3 步**：等待 2-3 分钟

脚本会自动完成所有操作。

---

## 🆘 常见问题

### Q1: 怎么知道 SSH 连接的是哪个端口？

**A**: SSH 默认用端口 **22**。如果不指定，就是 22。

如果你知道 SSH 用的是其他端口（比如 2222），可以这样：
```bash
ssh -p 2222 huangshengwei@192.168.1.112
```

### Q2: 如果连不上 SSH，怎么办？

**A**: 按这个顺序排查：
1. 检查网络：`ping 192.168.1.112` 能通吗？
2. 检查 Mac 防火墙：System Preferences → Security & Privacy → Firewall
3. 检查服务器防火墙或路由器设置
4. 尝试用其他端口（问服务器管理员）

### Q3: 密码有特殊字符，怎么输入？

**A**: 直接输入即可，不需要转义。特殊字符会被正确识别。

### Q4: 怎么保存连接信息，下次快速连接？

**A**: 编辑 SSH 配置文件：

```bash
nano ~/.ssh/config
```

添加这些行：
```
Host market-server
    HostName 192.168.1.112
    User huangshengwei
    Port 22
```

保存（`Ctrl + X`，然后 `Y`，`Enter`）

下次连接就可以简单地：
```bash
ssh market-server
```

---

## 📊 连接方式对比

| 方式 | 简单度 | 功能 | 适合场景 |
|------|--------|------|---------|
| 浏览器 | ⭐⭐⭐⭐⭐ | 查看网页 | 快速查看服务器状态 |
| SSH 终端 | ⭐⭐⭐ | 完整命令行 | 执行部署、管理系统 |
| SFTP (Finder) | ⭐⭐⭐ | 文件管理 | 上传下载文件 |
| Cyberduck | ⭐⭐⭐ | 文件管理 | 友好的文件操作 |
| VS Code | ⭐⭐ | 完整开发 | 编辑和调试代码 |

---

## ✅ 推荐方案

**如果你现在要部署**：
1. 打开终端
2. 执行 SSH 连接
3. 粘贴部署命令
4. 完成！（5 分钟内）

**如果你要经常上传文件**：
1. 用 Cyberduck 或 Finder (SFTP)
2. 拖拽文件即可
3. 无需命令行

---

## 🎯 立即开始

选择一个适合你的方式，告诉我连接时遇到的任何问题，我会帮你排查！

需要帮助吗？
