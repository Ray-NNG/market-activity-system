# 售前讲师表格列宽设置功能指南

## 🎯 功能简介
这是一个简单的列宽设置功能，允许您调整售前讲师表格中每一列的宽度，调整满意后保存设置。

## 📍 如何访问列宽设置

### 方法一：表格上方的"设置"按钮
1. 进入"售前讲师运营"页面
2. 在表格容器上方，可以看到一个"设置"按钮（齿轮图标）
3. 点击"设置"按钮打开列宽设置弹窗

### 方法二：直接访问
- 页面加载完成后，可以在表格右上角找到"设置"按钮
- 点击即可打开设置界面

## ⚙️ 设置界面说明

### 设置弹窗包含：
1. **列名称列表**：显示表格中的所有列（共14列）
2. **宽度滑块**：每列对应一个滑块，可调整宽度（40-300px）
3. **当前宽度显示**：显示当前列的宽度值
4. **默认宽度**：显示列的默认宽度作为参考
5. **预览区域**：实时预览调整后的列宽效果

### 可调整的列：
1. 复选框列（34px）
2. 始出日期（90px）
3. 地市（70px）
4. 销售（65px）
5. 公司（130px）
6. 对接人（65px）
7. 类型（75px）
8. 层级（默认宽度）
9. 售前（默认宽度）
10. 商机（50px）
11. 商机信息补充（150-250px）
12. 落地金额（85px）
13. 入校渠道（80px）
14. 特别备注（120-200px）
15. 操作（50px）

## 🔄 操作流程

### 标准流程：
1. **打开设置**：点击表格上方的"设置"按钮
2. **调整列宽**：拖动每列的滑块调整宽度
3. **实时预览**：查看表格预览区域的列宽变化
4. **保存设置**：调整满意后点击"保存设置"
5. **应用效果**：点击"应用当前设置"按钮将设置应用到表格

### 重置设置：
- 点击"重置为默认值"恢复所有列的默认宽度
- 点击"关闭设置"不保存任何更改直接关闭

## 💾 保存机制

### 保存位置：
- 设置保存在 `localStorage` 中的 `ps-column-widths` 键
- 格式为JSON对象，包含所有列的宽度值
- 页面刷新后自动读取并应用已保存的设置

### 应用方式：
1. 自动应用：页面加载时自动应用已保存的列宽
2. 手动应用：调整后点击"应用当前设置"立即生效

## 🎨 使用建议

### 不同场景推荐设置：

#### 1. 数据密集型查看
```
公司列：180px
特别备注：200px
商机信息补充：250px
```
**特点**：便于查看详细内容

#### 2. 紧凑布局
```
公司列：100px
对接人：55px
销售：55px
```
**特点**：在同一屏幕内显示更多列

#### 3. 平衡布局（默认）
```
保持默认宽度
```
**特点**：视觉平衡，适合大多数场景

### 高级技巧：
1. **重点突出**：将重要列（如"公司"、"类型"）设置较宽，便于识别
2. **信息完整**：将内容列（如"特别备注"、"商机信息补充"）设置足够宽度，避免内容截断
3. **快速对比**：相关列设置相似宽度，便于横向对比

## 🔧 技术实现细节

### 保存机制：
- 使用 `localStorage.setItem('ps-column-widths', JSON.stringify(widths))` 保存
- 页面加载时使用 `JSON.parse(localStorage.getItem('ps-column-widths'))` 读取
- 宽度值以像素为单位存储

### 动态更新：
- 使用 `tableEl.style.minWidth = 'auto'` 解除固定宽度
- 通过 `th.style.width` 和 `td.style.width` 动态设置列宽
- 更新后重新计算表格总宽度

### 列宽限制：
- 最小宽度：40px（避免列过窄无法操作）
- 最大宽度：300px（避免列过宽影响布局）
- 默认宽度：根据列内容类型预设

## 🚀 快速使用

### 只需三步：
1. 点击"设置"按钮
2. 调整滑块到满意的宽度
3. 点击"保存设置"并"应用当前设置"

### 支持的操作：
- 单个列宽调整
- 批量调整多列宽度
- 保存个人偏好设置
- 恢复系统默认设置

## ⚠️ 注意事项

### 浏览器兼容性：
- 需要现代浏览器支持localStorage
- 确保浏览器支持滑块控件（input[type="range"]）

### 数据安全：
- 设置仅保存在本地浏览器
- 更换浏览器或清除缓存会丢失设置
- 建议记录理想宽度配置

### 性能影响：
- 实时预览对性能影响极小
- 建议避免过于频繁的调整操作

## 📞 技术支持

### 遇到问题？
1. **设置不生效**：检查浏览器是否支持localStorage
2. **滑块不工作**：确认浏览器支持HTML5滑块控件
3. **想恢复默认**：使用"重置为默认值"功能

### 反馈建议：
- 记录下您的理想列宽配置
- 如有特殊需求，可以进一步定制功能
- 欢迎提供使用体验反馈

---

**✨ 现在您可以完全控制表格的列宽布局，创建最适合您工作习惯的表格视图！**